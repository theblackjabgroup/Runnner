{% schema %}
{
  "name": "Scrolling Marquee",
  "settings": [
    {
      "type": "text",
      "id": "marquee_text",
      "label": "Marquee Text",
      "default": "SUBSCRIBE FOR EMAILS→"
    },
    {
      "type": "range",
      "id": "scroll_speed",
      "label": "Scroll Speed",
      "min": 10,
      "max": 60,
      "step": 5,
      "default": 30,
      "info": "Lower number = faster scroll"
    }
  ],
  "presets": [
    {
      "name": "Scrolling Marquee"
    }
  ]
}
{% endschema %}
<div class="w-full overflow-hidden bg-white border border-black" {{ section.shopify_attributes }}>
  <div class="marquee-container flex whitespace-nowrap">
    {% assign marquee_text = section.settings.marquee_text | strip %}
    {% assign scroll_speed = section.settings.scroll_speed | default: 30 %}
    
<div class="marquee-content flex flex-row text-black text-[16px] md:text-[25px] lg:text-[25px] font-semibold py-0 h-15 md:-mt-[4px]">
      {% for i in (1..10) %}
        <span class="px-2">{{ marquee_text }}</span>
      {% endfor %}
    </div>
    
    <!-- Duplicate content for seamless looping -->
<div class="marquee-content flex flex-row text-black text-[16px] md:text-[25px] lg:text-[25px] font-semibold py-0 h-15 md:-mt-[4px]">
      {% for i in (1..10) %}
        <span class="px-2">{{ marquee_text }}</span>
      {% endfor %}
    </div>
  </div>
</div>

<style>
  .marquee-container {
    display: flex;
    animation: scroll-x {{ section.settings.scroll_speed | default: 30 }}s linear infinite;
  }
  
  @keyframes scroll-x {
    0% {
      transform: translateX(-50%);
    }
    100% {
      transform: translateX(0);
    }
  }
</style>
