name: Shopify Lighthouse CI

on: [push]

jobs:
  lhci:
    name: Lighthouse (PF_TESTING)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Lighthouse
        uses: shopify/lighthouse-ci-action@v1
        with:
          access_token: ${{ secrets.APIKEY }}
          store: "delhi-dev-themetest.myshopify.com"
          password: "shptka_1eb99914058996859e0d6b842538df50"
          lhci_min_score_categories_accessibility: 0.9
          lhci_min_score_categories_performance: 0.7
