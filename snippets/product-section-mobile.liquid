{%- comment -%}
  Product Section - Mobile Version

  Complete mobile product page including:
  - Mobile gallery with thumbnails
  - Product information and variants
  - Tab system (Description, Details, Sizing)
  - Add to cart functionality
  - Delivery options
  - Quantity selector
  - Pricing and savings
  - Pickup availability

  Parameters:
  - product: Product object (required)
  - section: Section object for settings and blocks (required)
{%- endcomment -%}

<section
  class="lg:hidden flex flex-col gap-4 px-4 py-4 product-section-container"
  role="main"
  aria-labelledby="mobile-product-title"
>
  <!-- Mobile Image Gallery -->
  {% render 'product-gallery-mobile', product: product %}

  <!-- Mobile Product Details -->
  <div class="details-inner flex flex-col" role="region" aria-labelledby="mobile-product-title">
    {% render 'product-header-mobile', product: product %}
    {% render 'product-tabs-mobile', product: product, blocks: section.blocks %}
    {% render 'product-purchase-mobile', product: product, section: section %}
  </div>
</section>
